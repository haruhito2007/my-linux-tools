# my-linux-tools
Linuxの授業で作成した自作コマンド集です。

## showpath
PATHの中身を1行ずつ表示します。

## originalalias
alias lpw="ls -l;pwd;whoami"
ディレクトリの中身と場所とユーザーを表示済ます。

## じつは
ここで編集ができます

検索するコマンド
find . -type d -name ".git"
